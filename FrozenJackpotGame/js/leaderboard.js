/* Leaderboard stub (deprecated).
   The full leaderboard implementation was removed during cleanup.
   This stub preserves imports and provides no-op methods so existing imports don't break.
*/
export default class Leaderboard {
  constructor(){
    console.warn('Leaderboard stub in place — no-op');
  }
  addEntry(){ /* no-op */ }
  getTop(){ return []; }
  clear(){ /* no-op */ }
}
